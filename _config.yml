# GitHub Pages. Source: Settings -> Pages -> Deploy from branch, main / (root).
# README.md becomes the landing page, HOWTO.md is served at /HOWTO.
# Relative links between the markdown files keep working: GitHub Pages
# enables jekyll-relative-links by default.
title: Mealie skill
description: Clean up a Mealie instance through its REST API - an LLM makes the judgement calls, a script does every write.
theme: jekyll-theme-cayman
exclude:
  - dist
  - __pycache__
